'use client'
import { createSlice } from '@reduxjs/toolkit'

const initialState = {
}

export const admin = createSlice({
  name: 'admin',
  initialState,
  reducers: {
  }
})

export const {

} = admin.actions
export default admin.reducer